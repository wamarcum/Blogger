<h1>All Tags</h1>

<ul id="tags">
	<% @tags.each do |tag| %>
		<li>
			<%= link_to tag.name, tag_path(tag) %>
		</li>
	<% end %>


<p> <%=link_to "Create a New Post", new_article_path, class: "new_article" %></p>
</ul>

